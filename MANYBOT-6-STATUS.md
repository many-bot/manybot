# ManyBot 6 — Status (08/26/2026)

## Current State

* Phases 1-11: **complete**. The v6 architecture is shipped and stable in production for both 5.x and the in-development 6.x line.
* `npm run typecheck && npm run lint && npm test`: **233/233 pass, 10 integration tests skipped (require `MANYBOT_RUN_WHATSAPP_TESTS=1` + live session), 0 fail** — green as of v5.9.0.
* The `scripts/check-types-drift.ts` step added to `npm run check` flags ~36 cosmetic "type mismatches" between `pluginApi.ts` internal interface names (`IConfig`/`IAdmin`/`ISend`/...) and `@manybot/types` published names (`ConfigApi`/`AdminApi`/`SendApi`/...) — these are intentional renames (`MANYBOT-6.md` Phase 10), structurally identical, not real drift. The script's `isTypeAssignableTo` comparison is nominal rather than structural despite its docstring; fixing the script to walk property shapes recursively is queued as a pre-existing tech-debt item, not part of this release.

## Pending

* Phase 3's hard enforcement of "every command must be in `commands.yaml`" — the plan itself defers this ("Enforcement will happen once YAML becomes the primary path, rather than opt-in") and the file's own top warning requires 5.x to keep working exactly as it does now; flipping this now would be a breaking change, not a bug fix.
* Phase 3's dual-use conflict warning (same function referenced in YAML `function:` AND called directly via `ctx.plugins.require(...)` by another plugin) — safe to add (additive, warning-only) but needs a design pass: static analysis of plugin source isn't practical, so this would have to be a runtime check inside `ctx.plugins.require()`/`ctx.plugins.get()` comparing the requested function name against the set of function names registered as `function:` targets in the command registry.
* `scripts/check-types-drift.ts` — fix the structural comparison to walk property shapes recursively instead of relying on `isTypeAssignableTo` (which is nominal and trips on intentional renames).

## Files Touched (all sessions, cumulative)

`commandsConfig.ts` (import mechanism) · `commandsConfig.test.ts` (import tests) · `locales/{en,pt,es}.json` (import log keys) · `commandAccess.ts` · `commandAccess.test.ts` · `commandRegistry.ts` · `pluginApi.ts` · `drivers/baileys/api/index.ts` · `runCommand.ts` · `runCommand.test.ts` · `pluginGuard.ts` · `drivers/baileys/messageHandler.ts` · `drivers/baileys/messageHandler.test.ts` · `kernel/chatSession.ts` (new) · `kernel/chatSession.test.ts` (new) · `kernel/commandMenu.ts` · `kernel/commandMenu.test.ts` · `locales/{en,pt,es}.json` · `logger/logger.ts` · `logger/logger.test.ts` (new) · `config.ts` · `config.test.ts` · `main.ts` · `kernel/pluginLoader.ts` · `packages/types/{en,pt}/index.d.ts` (real hand-maintained package, v1.5.0 → v1.7.0: added `CommandsApi`, `SessionApi`, `RunCommandResult`+`runCommand`; v1.7.0 added phone-number enrichment on `IContact`: `number`/`numberRaw`/`numberPretty`/`country`/`countryCallingCode`, plus nullable `id`) · `packages/types/{package.json,README.md}` · `package.json` (`build:types` validates the package; `check` includes the types-drift step) · `client/banner.ts` · `client/banner.test.ts` (new) · `drivers/baileys/index.ts` (banner gating, integration plugin auto-load on opt-in) · `drivers/baileys/adapter.ts` (LID↔PN cache feeds from `groupMetadata`, `group-participants.update`, `group.join-request`; `mentionedJid` normalized through the cache) · `drivers/baileys/api/index.ts` (`buildContactsApi` now exported, `normalizeContact` returns LID-first `id` + phone-number fields) · `drivers/baileysAdapter.test.ts` (LID↔PN cache tests on group-participants.update) · `client/store.ts` (added `pnMap` reverse LID↔PN cache; `pnMap` reconstructed from `lidMap` on hydrate) · `utils/phoneNumber.ts` (new, libphonenumber-js wrapper) · `utils/phoneNumber.test.ts` (new) · `plugins/__manybot_integration__/index.ts` (now uses `getTestConfig()` so it agrees with `integrationMode`'s gate) · `plugins/__manybot_integration__/index.test.ts` (regression test for the gate/handler agreement) · `kernel/testConfig.ts` (cache + `_resetTestConfigForTests` + JSDoc on what shape `normalizeTestChat` accepts) · `kernel/waContract.ts` (`GroupParticipantsUpdateEvent.action` gains `"modify"` for Baileys v7) · `kernel/pluginLoader.ts` (`getGlobalKernelRefs()` escape hatch for the integration harness) · `drivers/baileys/api/contacts.integration.test.ts` (new opt-in real-WhatsApp suite) · `scripts/check-types-drift.ts` (new, structural drift detector) · `scripts/probe-contacts.mjs` (new, manual smoke probe) · `.npmrc` (new, commit-msg hook) · `CHANGELOG.md`

## Session History (condensed)

**08/18 → 08/19 (continuations 1-8) — Phases 1-4, 6, 7 built and wired.** `commandsConfig.ts`/`commandRegistry.ts` extended with `subcommands`/`arguments`/`group`/`categoryHiddenInScope`; `commandAccess.ts` (`ctx.commands`) and `runCommand.ts` (dispatch, permission chain, required-arg validation, Phase-8 `fireAlert`) built and wired into `messageHandler.ts` via `ctx.runCommand`; two real bugs found and fixed along the way (subcommand handler resolution, permission `fallbackScope` chain); `chatSession.ts` added for Phase 7 (`ctx.session`); Phase 6 native menu finished (pagination, welcome-message dispatcher, `categoryHiddenInScope` filtering). Ended at **207/207** tests.

**08/19 continuation 9 — Phase 9 (2/3): log levels + noise removal.** `logger.ts`: `LogLevel` (`normal`/`clean`/`minimal`) with `setLogLevel`/`getLogLevel`; `info` gated to `normal`, `success` hidden at `minimal`, `warn`/`error` always shown. `config.ts`: new `LOG_LEVEL` key (TOML, EN+PT defaults, validated like `SECURITY_LEVEL`). `main.ts` calls `setLogLevel(LOG_LEVEL)` on boot. `pluginLoader.ts`: per-plugin "Plugin Loaded/Reloaded" line demoted `info` → `debug` (still visible with `--debug`). 4 new tests (`logger.test.ts`) + 1 (`config.test.ts`). **211/211** tests. ASCII banner (3rd Phase-9 item) not done this session.

**08/19 continuation 10 — Phase 10, attempt 1 (later scrapped): auto-generator.** Built `scripts/generate-types.ts` (TS compiler API) to generate `packages/types/index.d.ts` from `pluginApi.ts`, with `// @manybot-types:ID` markers meant to preserve hand-edited JSDoc across reruns; driver-only types stubbed opaque. Worked and typechecked, wired into `npm run build`.

**08/20 continuation 11 — Phase 10, attempt 2 (final): real package adopted, gap closed by hand.** User supplied an existing hand-maintained `@manybot/types` v1.5.0 — real Baileys types via peer-dependency, curated public interface names (`SendApi`, `AdminApi`, `ChatContext`, etc.), full en+pt locale parity, rich `@example`/`@see` JSDoc, README. Far better than the auto-generated version, but predated this session's `pluginApi.ts` — missing `ctx.commands`/`ctx.session`/`ctx.runCommand`. Decision: scrap the generator (deleted `scripts/generate-types.ts`), adopt the real package as-is, hand-add the 3 missing pieces to both `en/index.d.ts` and `pt/index.d.ts` matching the existing style (`CommandsApi`+`CommandInfo` on `BaseApi`; `SessionApi` runtime-only; `RunCommandResult`+`runCommand()` runtime-only). Version 1.5.0 → 1.6.0 (additive, not breaking). Root `package.json`'s `build:types` changed from "regenerate" to "validate" (`tsc --noEmit -p packages/types/tsconfig.json`). Verified: package's own `npm run typecheck` clean; root `typecheck`/`lint` unaffected. **211/211** tests (untouched that session).

**08/20 continuation 12 — Phase 9 finished + CHANGELOG.md updated.** Found `printBanner()` (`src/client/banner.ts`) already existed and was already wired into `drivers/baileys/index.ts`, but unconditionally — printed on every reconnect, not just startup, and had no LOG_LEVEL gate at all (contradicted the plan's "optional feature that can be disabled for users who prefer clean logs"). Fixed both: `printBanner()` now returns early unless `getLogLevel() === "normal"`; a module-level `bannerShown` flag in `drivers/baileys/index.ts` ensures it only prints once per process, not on every reconnect. New `banner.test.ts` (3 tests, 100% coverage on `banner.ts`). `CHANGELOG.md`'s stale `Unreleased`/`Known limitations` section (still describing subcommands and welcome-message as *not* implemented, though both shipped in continuations 1-8) rewritten to reflect the actual current v6 feature set: unified dispatch (`ctx.runCommand`, subcommands, required-arg validation), crash alerts, exclusive chat session, refined native menu, log levels + banner, `@manybot/types` 1.6.0. **214/214** tests, `typecheck`/`lint` clean.

**08/20 continuation 13 — Phase 11 (release readiness) closed.** Two loose ends from the pre-release review: (1) `CHANGELOG.md`'s "Known limitations" still listed `categoryHiddenInScope` as unconsumed, but `commandMenu.ts` already filters by it at 4 call sites (closed in continuation 1-8) — stale line removed. (2) `runCommand.ts`'s unmatched-subcommand reply was a hardcoded English template literal, the only remaining non-i18n user-facing string in the unified dispatch path — replaced with `t("commandRun.unknownSubcommand", { sub, cmd, valid })`, new key added to `en`/`pt`/`es` locales mirroring the existing `commandRun.missingRequiredArg` pattern. No behavior change, no new tests needed (no test asserted the old hardcoded string). **214/214** tests, `typecheck`/`lint` clean.

**08/20 continuation 14 — Phase 1 closed: `import:` mechanism.** `commandsConfig.ts`: new root-level `import:` key (string or list of paths, resolved relative to `PATHS.HOME`); `resolveImports()` reads each referenced YAML file and folds its top-level keys into the main root object before `defaults`/`menu`/`categories`/`manuals`/`specs` are extracted — so an imported file can contribute a `menu:` block, `manuals:`, `categories:`, or full command entries exactly as if they were written inline in `commands.yaml`. Ownership is first-wins, no deep merge: a key already present (from the main file or an earlier import) that reappears in a later import is logged as an error and skipped, keeping the earlier definition; a nested `import:` inside an imported file is ignored with a warning (not recursive — out of scope); a missing/unreadable/malformed import file logs an error and is skipped without failing the rest of the load. Added `import` to `RESERVED_KEYS` so it's never mistaken for a command id. 5 new locale keys (`commandsConfigImport{ReadFailed,ParseFailed,InvalidRoot,Nested,KeyConflict}`) added to `en`/`pt`/`es`. 4 new tests in `commandsConfig.test.ts` (merge from multiple imports, single-path-not-list form, key-conflict-keeps-first, missing/malformed import doesn't fail the load) — **pending local validation** (dev sandbox has no `node_modules`/network); expect 218/218 if these pass as written.

**08/26 continuation 15 — v5.9.0 (Baileys v7 + LID-aware contacts + integration suite).** Major dependency bump: `@whiskeysockets/baileys` 6.7.24 → `^7.0.0-rc14` (LID became the internal addressing mode mid-2026; v7 surfaces it in `GroupMetadata.participants` as `id` + optional `phoneNumber`/`lid`, and adds a `"modify"` action on `group-participants.update` and `authorPn`/`participantPn` fields on `group.join-request`). New `pnMap` cache in `src/client/store.ts` (reverse of `lidMap`): PN→LID lookups, hydrated from `lidMap` on startup so existing saved sessions keep their mappings, fed passively by `groupMetadata`, `group-participants.update`, and `group.join-request` payloads whenever both forms appear in the same event. `normalizeContact()` rewritten to return LID-first `id` (null when no LID mapping known) plus 5 new phone-number fields (`number` E.164 / `numberRaw` / `numberPretty` / `country` ISO 3166-1 alpha-2 / `countryCallingCode` ITU), powered by new `src/utils/phoneNumber.ts` wrapper around `libphonenumber-js` (also new dep, ^1.13.11). `mentionedJid` normalized through the LID↔PN cache in the adapter so outbound mentions stay in the canonical LID form. `@manybot/types` bumped 1.6.0 → 1.7.0 to publish the new shape. New opt-in integration suite at `src/drivers/baileys/api/contacts.integration.test.ts` (round-trips marker messages against `TEST_CHAT` and asserts the new `IContact` shape against a real account; skips cleanly when the integration gate isn't satisfied) + manual smoke probe `scripts/probe-contacts.mjs`. Integration plugin now auto-loads after `setupPlugins()` when `MANYBOT_RUN_WHATSAPP_TESTS=1` is set; the previous manual `MANYBOT_TEST_CHAT` env read in its `setup()` was replaced with a `getTestConfig()` call so it agrees with `kernel/integrationMode.ts`'s gate (regression test added). Welcome-message dispatcher in `messageHandler.ts` gained two gates (`!msg.fromMe`, `!chat.isGroup`) to fix a real bug where history-sync replays of the bot's own outgoing messages were triggering a welcome in the bot's own DM on every reconnect; 5 new tests in `messageHandler.test.ts` cover the regression. `buildContactsApi` is now `export`ed so the integration suite can call `contacts.get(...)` from outside the plugin context. `getGlobalKernelRefs()` escape hatch in `pluginLoader.ts` for the integration harness only. `scripts/check-types-drift.ts` added to `npm run check` — flags pre-existing nominal-vs-structural drift (cosmetic, see Current State). `.npmrc` added (conventional-commit message footer). Final state: **233/233 pass, 10 integration skipped, 0 fail**, `typecheck`/`lint` clean.
